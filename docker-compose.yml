version: '3'
services:
  llmcord:
    build:
      context: .
      dockerfile: Dockerfile
    image: llmcord
    container_name: llmcord
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ${MY_WEIGHTS}:/usr/src/llmcord/weights